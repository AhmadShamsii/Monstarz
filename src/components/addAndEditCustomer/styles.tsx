import styled from "styled-components";
import { Form, Typography } from "antd";
const { Text } = Typography;

export const StyledText = styled(Text)`
  position: relative;
  top: -63px;
  left: 310px;
  color: blue;
`;
export const StyledFormButtons = styled(Form.Item)`
  position: relative;
  right: 160px;
  bottom: -71px;
`;
